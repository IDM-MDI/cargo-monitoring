package by.ishangulyyev.backend.repository;

import by.ishangulyyev.backend.entity.AcceptedCargo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AcceptedCargoRepository extends JpaRepository<AcceptedCargo, String> {
}
